A single cli application could offer the following services:

- generate croissant files for datasets
- generate custom croissant file for BioCypher adapters
- Start a web application to create custom croissant file for BioCypher adapters
- validate croissant files
  - general croissant files
  - specific schema for BioCypher adapters
- fetch adapter metadata from local and remote repositories
  - it needs a file or database to consult the location of each metadata resource
- generate an entry into the registry.
  - it could be a file or database
- list all the successful BioCypher adapters registered until now
